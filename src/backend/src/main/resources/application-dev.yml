server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/master_interview
    username: postgres
    password: quanbro7
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

    properties:
      hibernate:
        format_sql: true